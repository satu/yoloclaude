# Copy to .env and adjust per host.
USERNAME=dariofreni
USER_ID=1000
GROUP_ID=1000
CONTAINER_NAME=yoloclaude
# Host docker GID — only relevant if the docker.sock bind is re-enabled in
# compose.yaml. Find it with: stat -c %g /var/run/docker.sock
DOCKER_GID=998
MEMORY_LIMIT=16g
CPU_LIMIT=4
